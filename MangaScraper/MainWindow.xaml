<Window x:Class="MangaScraper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MangaScraper"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid>
        <StackPanel>
            <materialDesign:Card Padding="32" Foreground="{DynamicResource SecondaryHueMidBrush}">
                <TextBlock Style="{DynamicResource MaterialDesignHeadline6TextBlock}"><Run Text="My First Material Design App"/></TextBlock>
            </materialDesign:Card>
            <TreeView x:Name="TreeView" Height="100">
                <TreeView.Resources>
                    
                </TreeView.Resources>
            </TreeView>
        </StackPanel>
    </Grid>
</Window>
